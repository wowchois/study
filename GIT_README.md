### Git 

```
git clone repository_url
git branch branch_name     -- local에 생성
git checkout branch_name   -- branch 접속
```

```
git add .   -- 수정한 파일 모두 추가
git commit -m "message" -- commit
git push origin branch_name  -- branch로 push한다.

```
